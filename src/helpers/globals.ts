// Size in world units for carousel items
export const CARD_SIZE: { width: number; height: number } = {
  width: 12,
  height: 12,
};
export const CARD_SPACING = 0.2;

// Min/max camera offset from the outer edge of the carousel
export const CAMERA_OFFSET_MIN = 15;
export const CAMERA_OFFSET_MAX = 65;

// Random seed for consistent colors
export const RANDOM_COLOR_SEED = "r4nd0mn3ss";
